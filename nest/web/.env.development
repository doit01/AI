# ── 前端开发环境 ──
# Vite 自动加载 .env.development (vite dev) / .env.production (vite build)
# 只有 VITE_ 前缀的变量会暴露给客户端

VITE_API_BASE=/api
VITE_API_TIMEOUT=15000
VITE_DEV_PORT=5173
VITE_PROXY_TARGET=http://localhost:3000
